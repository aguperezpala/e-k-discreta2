#ifndef __DSATUR_H
#define __DSATUR_H

#include "node.h"
#include "../const.h"

/* El node debe ser el nodo con mayor grado de todos
 * para que cumpla con dsatur.
 */
int dsatur(node_t node , node_t * nodes , short delta)

#endif
